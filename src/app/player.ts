import { PerfPart } from "./perf-part";

export class Player {
  playerlogin: string;
  txp: number;
  slotengine: PerfPart;
  slottransmission: PerfPart;
  slottires: PerfPart;
  inventory: PerfPart[];
}
